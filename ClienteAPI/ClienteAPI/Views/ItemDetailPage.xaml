<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClienteAPI.Views.ItemDetailPage"
             Title="Detalhes do Produto">

    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Remover" Command="{Binding RemoveItemCommand}" />
    </ContentPage.ToolbarItems>
    
    <StackLayout Spacing="20" Padding="15">
        <Label Text="{Binding Produto_Id}" FontSize="Large" FontAttributes="Bold" Padding="10"  />
        <Label Text="{Binding Nome_Produto}" FontSize="Large" FontAttributes="Bold" Padding="10" />
        <Label Text="{Binding Valor_Unitario}" FontSize="Large" FontAttributes="Bold" Padding="10" />
        <Label Text="{Binding Qtde_Estoque}" FontSize="Large" FontAttributes="Bold" Padding="10"  />
        <Label Text="{Binding Data_Ultima_Compra}" FontSize="Large" FontAttributes="Bold" Padding="10"  />
        <Label Text="{Binding Valor_Ultima_Compra}" FontSize="Large" FontAttributes="Bold" Padding="10"  />
    </StackLayout>
    
</ContentPage>